#pragma once

#include "PCH.h"

namespace Serialization {
	/**
	 * @brief Resolves addresses for serialization.
	 * This function is called during the initialization phase to set up necessary addresses
	 * for serialization tasks.
	 */
	void ResolveAddresses();
}